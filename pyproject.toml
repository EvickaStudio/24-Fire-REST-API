[build-system]
requires = ["setuptools", "wheel", "aiohttp", "requests>=2.32.2", "anyio>=4.4.0", "zipp>=3.19.1"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88
target-version = ['py38']


[tool.isort]
profile = "black"
